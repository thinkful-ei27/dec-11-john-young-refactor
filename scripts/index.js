/* global $, videoList */

'use strict';

// When DOM is ready:
$(function () {
  // TASK:
  // 1. Run `bindEventListeners` to bind the event listener to the DOM
  videoList.bindEventListeners();
  videoList.render();
});
